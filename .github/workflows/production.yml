name: Deploy Production

on:
  push:
    branches: [main]

jobs:
  deploy:
    name: Migrate DB + Deploy to Cloudflare Pages (production)
    runs-on: ubuntu-latest
    environment: production
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm

      - name: Install dependencies
        run: npm ci

      - name: Lint
        run: npm run lint --workspace=@eqbis/web

      - name: Typecheck
        run: npx tsc --noEmit
        working-directory: apps/web

      - name: Run DB migrations (Neon production branch)
        run: npm run db:migrate
        working-directory: packages/db
        env:
          DATABASE_URL: ${{ secrets.PROD_DATABASE_URL }}

      - name: Build for Cloudflare Pages
        run: npm run build:cf --workspace=@eqbis/web
        env:
          DATABASE_URL: ${{ secrets.PROD_DATABASE_URL }}
          BETTER_AUTH_SECRET: ${{ secrets.PROD_BETTER_AUTH_SECRET }}
          NEXT_PUBLIC_APP_URL: https://eqbis.com
          ROOT_DOMAIN: eqbis.com

      - name: Deploy to Cloudflare Pages (production / main branch)
        run: |
          npx wrangler pages deploy apps/web/.vercel/output/static \
            --project-name=eqbis-web \
            --branch=main \
            --commit-dirty=true
        env:
          CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
          CLOUDFLARE_ACCOUNT_ID: ${{ secrets.CLOUDFLARE_ACCOUNT_ID }}
